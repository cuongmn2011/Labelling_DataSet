# Session Log

Ghi lại những việc đã làm theo từng session làm việc.

---

## 2026-05-12

### Thiết lập tài liệu dự án
- Tạo `CLAUDE.md` tại root — mô tả tech stack, cấu trúc thư mục, quyết định kiến trúc, lệnh hay dùng.
- Tạo `.CLAUDE/plan.md` — DB schema đầy đủ (9 bảng), API endpoints, frontend routes, Phase roadmap 1–4, verification test cases.
- Tạo `.CLAUDE/RULE.md` — các rule bắt buộc: backend async, RBAC, file serving token, Konva 3-layer, TypeScript strict, migration, export, SSE, testing, commit convention.
- Tạo `.CLAUDE/SKILL.md` — 7 skill lặp lại: `new-migration`, `new-router`, `new-annotation-type`, `new-export-format`, `dev-start`, `seed-db`, `verify-phase`, `check-security`.

### Thiết lập auto-memory
- Tạo `memory/project_labeling_app.md` — thông tin project, stack, phase roadmap, known gaps.
- Tạo `memory/user_cuong.md` — profile người dùng.
- Tạo `memory/feedback_mandatory_rules.md` — rules hay bị bỏ sót từ RULE.md.
- Tạo `memory/reference_project_docs.md` — pointer tới 3 file tài liệu chính.
- Cập nhật `memory/MEMORY.md` index với 4 entries.

### Trạng thái project
Tất cả 4 Phase đều **chưa bắt đầu** — chỉ mới hoàn thành phần tài liệu và thiết lập.
